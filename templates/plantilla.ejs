<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reinvestar</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        font-size: 10px;
        line-height: 1.3;
      }
      table {
        border-collapse: collapse;
      }
      .main-table {
        background: #fff;
        /* Sin sombra ni borde redondeado */
        /* box-shadow: none; */
        /* border-radius: 0; */
        overflow: visible;
      }
      .header-logo {
        padding-right: 8px;
        width: 35%;
        vertical-align: middle;
      }
      .header-logo img {
        display: block;
        margin: 0 auto;
        max-width: 180px;
        height: auto;
        background: #fff;
      }
      .header-info {
        background-color: #ffc55a;
        padding: 8px;
        border: 1px solid #000;
        border-radius: 4px;
      }
      .section-title {
        background-color: #ffc55a;
        font-weight: bold;
        text-align: center;
        font-size: 11px;
        padding: 6px 0;
        border: 1px solid #000;
        border-radius: 4px;
      }
      .money-table td {
        padding: 2px 6px;
        font-size: 10px;
      }
      .money-table .label {
        text-align: left;
        width: 60%;
      }
      .money-table .money {
        text-align: right;
        width: 40%;
        white-space: nowrap;
      }
      .money-table .money .dollar {
        display: inline-block;
        width: 16px;
        text-align: right;
        font-weight: bold;
      }
      .money-table .money .amount {
        display: inline-block;
        min-width: 60px;
        text-align: right;
      }
      .signature-table td {
        padding: 4px 6px;
      }
      .footer {
        background-color: #000;
        color: #fff;
        text-align: center;
        font-weight: bold;
        font-size: 10px;
        padding: 10px;
        border: 1px solid #000;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <table cellpadding="0" cellspacing="0" border="0" style="width: 100%">
      <tr>
        <td align="center">
          <!-- Contenedor principal -->
          <table class="main-table" cellpadding="0" cellspacing="0" border="0" style="width: 750px; max-width: 100%;">
            <!-- Header -->
            <tr>
              <td class="header-logo">
                <img src="<%= logo %>" alt="Logo" width="200">
              </td>
              <td class="header-info">
                <table cellpadding="0" cellspacing="0" border="0" style="width: 100%">
                  <tr>
                    <td>
                      <span id="advisorFullName"><%= solicitud.advisorFullName %></span> (<span id="advisorId"><%= solicitud.advisorId %></span>)<br>
                      <span id="advisorPhone"><%= solicitud.advisorPhone %></span><br>
                      <a id="advisorEmail" href="mailto:<%= solicitud.advisorEmail %>" style="color: #000; text-decoration: none"><%= solicitud.advisorEmail %></a>
                    </td>
                  </tr>
                  <tr>
                    <td>
                      <a href="https://www.reinvestarcapitalgroup.com" style="color: #000; text-decoration: none">WWW.REINVESTARCAPITALGROUP.COM</a>
                    </td>
                  </tr>
                </table>
              </td>
            </tr>
            <!-- Espaciado -->
            <tr><td colspan="2" style="height: 10px"></td></tr>
            <!-- Título -->
            <tr>
              <td colspan="2" class="section-title">LETTER OF INTENT FOR SHORT TERM LOAN</td>
            </tr>
            <!-- Espaciado -->
            <tr><td colspan="2" style="height: 8px"></td></tr>
            <!-- Contenido Principal -->
            <tr>
              <!-- Columna izquierda -->
              <td style="vertical-align: top; width: 48%; padding-right: 3px">
                <!-- Borrower Info -->
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr class="section-title"><td colspan="2">BORROWER INFORMATION</td></tr>
                  <tr><td class="label">BORROWER INFORMATION</td><td><span id="borrowerName"><%= solicitud.borrowerName %></span></td></tr>
                  <tr><td class="label">LEGAL STATUS</td><td><span id="legalStatus"><%= solicitud.legalStatus %></span></td></tr>
                  <tr><td class="label">ISSUED DATE</td><td><span id="issuedDate"><%= solicitud.issuedDate %></span></td></tr>
                  <tr><td class="label">SUBJECT PROPERTY ADDRESS</td><td><span id="propertyAddress"><%= solicitud.propertyAddress %></span></td></tr>
                  <tr><td class="label">ESTIMATED FICO SCORE</td><td><span id="ficoScore"><%= solicitud.ficoScore %></span></td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- Loan Details -->
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr class="section-title"><td colspan="2">LOAN DETAILS</td></tr>
                  <tr><td class="label">LOAN TYPE</td><td><span id="loanType"><%= solicitud.loanType %></span></td></tr>
                  <tr><td class="label">PROPERTY TYPE</td><td><span id="propertyType"><%= solicitud.propertyType %></span></td></tr>
                  <tr><td class="label">ESTIMATED CLOSING DATE</td><td><span id="closingDate"><%= solicitud.closingDate %></span></td></tr>
                  <tr><td class="label">INTEREST RATE STRUCTURE</td><td><span id="interestStructure"><%= solicitud.interestStructure %></span></td></tr>
                  <tr><td class="label">LOAN TERM</td><td><span id="loanTerm"><%= solicitud.loanTerm %></span> months</td></tr>
                  <tr><td class="label">PREPAYMENT PENALTY</td><td><span id="prepaymentPenalty"><%= solicitud.prepaymentPenalty %></span></td></tr>
                  <tr><td class="label">MAXIMUM LTV</td><td><span id="maxLTV"><%= solicitud.maxLTV %></span>%</td></tr>
                  <tr><td class="label">MAXIMUM LTC</td><td><span id="maxLTC"><%= solicitud.maxLTC %></span>%</td></tr>
                </table>
                <div style="height: 8px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr class="section-title"><td colspan="2">TRANSACTION SUMMARY</td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- PURCHASE, REFINANCE, CASH-OUT -->
                <table class="money-table" cellpadding="2" cellspacing="0" border="0" style="width: 100%;">
                  <tr class="section-title"><td colspan="2">PURCHASE, REFINANCE, CASH-OUT</td></tr>
                  <tr><td class="label">As-is Value</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.asIsValue === 'number' ? solicitud.asIsValue.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.asIsValue %></span></td></tr>
                  <tr><td class="label">Original Acquisition Price</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.acquisitionPrice === 'number' ? solicitud.acquisitionPrice.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.acquisitionPrice %></span></td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- FIX & FLIP, GROUND UP CONSTRUCTION -->
                <table class="money-table" cellpadding="2" cellspacing="0" border="0" style="width: 100%;">
                  <tr class="section-title"><td colspan="2">FIX & FLIP, GROUND UP CONSTRUCTION</td></tr>
                  <tr><td class="label">Land or Acquisition Cost</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.landCost === 'number' ? solicitud.landCost.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.landCost %></span></td></tr>
                  <tr><td class="label">Financed Construction / Rehab Budget</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.constructionBudget === 'number' ? solicitud.constructionBudget.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.constructionBudget %></span></td></tr>
                  <tr><td class="label">Total Cost</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.totalCost === 'number' ? solicitud.totalCost.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.totalCost %></span></td></tr>
                  <tr><td class="label">Estimated After Completion Value</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.afterCompletionValue === 'number' ? solicitud.afterCompletionValue.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.afterCompletionValue %></span></td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- LOAN CLOSING COSTS -->
                <table class="money-table" cellpadding="2" cellspacing="0" border="0" style="width: 100%;">
                  <tr class="section-title"><td colspan="2">LOAN CLOSING COSTS</td></tr>
                  <tr><td class="label">Fee</td><td><span id="feePercent"><%= solicitud.feePercent %></span>%</td></tr>
                  <tr><td class="label">Origination Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.originationFee === 'number' ? solicitud.originationFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.originationFee %></span></td></tr>
                  <tr><td class="label">Underwriting Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.underwritingFee === 'number' ? solicitud.underwritingFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.underwritingFee %></span></td></tr>
                  <tr><td class="label">Processing Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.processingFee === 'number' ? solicitud.processingFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.processingFee %></span></td></tr>
                  <tr><td class="label">Servicing Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.servicingFee === 'number' ? solicitud.servicingFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.servicingFee %></span></td></tr>
                  <tr><td class="label">Legal Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.legalFee === 'number' ? solicitud.legalFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.legalFee %></span></td></tr>
                  <tr><td class="label">Appraisal Fee</td><td><span id="appraisalFee"><%= solicitud.appraisalFee %></span></td></tr>
                  <tr><td class="label">Budget review and Permit Validation report</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.budgetReview === 'number' ? solicitud.budgetReview.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.budgetReview %></span></td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- OTHER EXPENSES (Estimated) -->
                <table class="money-table" cellpadding="2" cellspacing="0" border="0" style="width: 100%;">
                  <tr class="section-title"><td colspan="2">OTHER EXPENSES (Estimated)</td></tr>
                  <tr><td class="label">Fee</td><td><span id="brokerFeePercent"><%= solicitud.brokerFeePercent %></span>%</td></tr>
                  <tr><td class="label">Broker Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.brokerFee === 'number' ? solicitud.brokerFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.brokerFee %></span></td></tr>
                  <tr><td class="label">Transaction Fee</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.transactionFee === 'number' ? solicitud.transactionFee.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.transactionFee %></span></td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- OTHER EXPENSES (Estimated) -->
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #000; font-weight: bold; text-align: start;"><td style="color: #fff; width: 70%">All terms subject to confirmation including but</td></tr>
                  <tr><td style="height: 5px"></td></tr>
                </table>
                <div style="height: 8px"></div>
                <!-- SIGNATURES -->
                <table class="signature-table" cellpadding="2" cellspacing="0" border="0" style="width: 100%;">
                  <tr class="section-title"><td style="width: 60%">BORROWER SIGNATURE:</td><td style="width: 40%"><span id="borrowerSignature"><%= solicitud.borrowerSignature %></span></td></tr>
                  <tr class="section-title"><td>BORROWER NAME:</td><td><span id="borrowerNameSignature"><%= solicitud.borrowerNameSignature %></span></td></tr>
                  <tr class="section-title"><td>ENTITY NAME:</td><td><span id="entityName"><%= solicitud.entityName %></span></td></tr>
                  <tr class="section-title"><td>DATE:</td><td><span id="date"><%= solicitud.date %></span></td></tr>
                </table>
              </td>
              <!-- Columna derecha -->
              <td style="vertical-align: top; width: 48%; padding-left: 3px">
                <!-- Loan Summary -->
                <table class="money-table" cellpadding="2" cellspacing="0" border="0" style="width: 100%;">
                  <tr class="section-title"><td colspan="2">LOAN SUMMARY</td></tr>
                  <tr><td class="label">TOTAL LOAN AMOUNT</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.totalLoanAmount === 'number' ? solicitud.totalLoanAmount.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.totalLoanAmount %></span></td></tr>
                  <tr><td class="label">ANNUAL INTEREST RATE</td><td><span id="interestRate"><%= solicitud.interestRate %></span>%</td></tr>
                  <tr><td class="label">REQUESTED LEVERAGE</td><td><span id="requestedLeverage"><%= solicitud.requestedLeverage %></span>%</td></tr>
                  <tr><td class="label">APPROX. MONTHLY INTEREST PAYMENT</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.monthlyInterest === 'number' ? solicitud.monthlyInterest.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.monthlyInterest %></span></td></tr>
                  <tr class="section-title"><td colspan="2">IF LOAN IS FIX & FLIP OR GROUND UP CONSTRUCTION</td></tr>
                  <tr><td class="label">CONSTRUCTION HOLDBACK</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.constructionHoldback === 'number' ? solicitud.constructionHoldback.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.constructionHoldback %></span></td></tr>
                  <tr><td class="label">INITIAL FUNDING</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.initialFunding === 'number' ? solicitud.initialFunding.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.initialFunding %></span></td></tr>
                  <tr><td class="label">DAY 1 APPROX. MONTHLY INTEREST PAYMENT</td><td class="money"><span class="dollar">$</span><span class="amount"><%= typeof solicitud.day1Interest === 'number' ? solicitud.day1Interest.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.day1Interest %></span></td></tr>
                </table>
                <div style="height: 6px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td colspan="2">LOAN METRICS</td></tr>
                </table>
                <div style="height: 6px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td colspan="2">PURCHASE, REFINANCE, CASH-OUT</td></tr>
                  <tr><td style="width: 60%">Loan to As-is Value (LTV)</td><td style="width: 40%"><span id="loanToAsValueLTV"><%= solicitud.loanToAsValueLTV %></span></td></tr>
                  <tr><td style="height: 3px"></td><td style="height: 3px"></td></tr>
                </table>
                <div style="height: 6px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td colspan="2">FIX & FLIP, GROUND UP CONSTRUCTION</td></tr>
                  <tr><td style="width: 60%">Loan to As-Is Value (LTAIV) %</td><td style="width: 40%"><span id="ltaivPercent"><%= solicitud.ltaivPercent %></span>%</td></tr>
                  <tr><td>Loan to As-Is Value (LTAIV) $</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="ltaivAmount"><%= typeof solicitud.ltaivAmount === 'number' ? solicitud.ltaivAmount.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.ltaivAmount %></span></td></tr>
                  <tr><td>Loan to Cost (LTC) %</td><td><span id="ltcPercent"><%= solicitud.ltcPercent %></span>%</td></tr>
                  <tr><td>Loan to Cost (LTC) $</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="ltcAmount"><%= typeof solicitud.ltcAmount === 'number' ? solicitud.ltcAmount.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.ltcAmount %></span></td></tr>
                  <tr><td>Loan to ARV (Maximum 75%) %</td><td><span id="larvPercent"><%= solicitud.larvPercent %></span>%</td></tr>
                  <tr><td>Loan to ARV (Maximum 75%) $</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="larvAmount"><%= typeof solicitud.larvAmount === 'number' ? solicitud.larvAmount.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.larvAmount %></span></td></tr>
                  <tr><td>Rehab Category</td><td><span id="rehabCategory"><%= solicitud.rehabCategory %></span></td></tr>
                </table>
                <div style="height: 6px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td colspan="2">CONDITIONS AND ADDITIONAL INFORMATION</td></tr>
                  <tr style="font-weight: normal; text-align: center"><td colspan="2">FOR SHORT TERM LOANS:</td></tr>
                  <tr><td style="width: 60%">1) Minimum Credit Score required</td><td style="width: 40%"><span id="minCreditScore"><%= solicitud.minCreditScore %></span></td></tr>
                  <tr><td>2) Commitment deposit</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="commitmentDeposit"><%= typeof solicitud.commitmentDeposit === 'number' ? solicitud.commitmentDeposit.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.commitmentDeposit %></span></td></tr>
                  <tr><td>(required to initiate loan processing and order of appraisal)</td><td></td></tr>
                  <tr style="text-align: start; height: 15px"><td colspan="2"><span style="font-weight: 800">If Loan doesn't close</span>, a statement of expenses will be produced, and only the remaining balance will be returned.</td></tr>
                </table>
                <div style="height: 6px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td colspan="2">MINIMUM BORROWER'S LIQUIDITY REQUIRED (Estimated)</td></tr>
                  <tr><td style="width: 60%">Cash to close (Down payment + closing costs)</td><td style="width: 40%; text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="cashToClose"><%= typeof solicitud.cashToClose === 'number' ? solicitud.cashToClose.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.cashToClose %></span></td></tr>
                  <tr><td>10% of Construction Budget</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="constructionBudgetPercent"><%= typeof solicitud.constructionBudgetPercent === 'number' ? solicitud.constructionBudgetPercent.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.constructionBudgetPercent %></span></td></tr>
                  <tr><td>6 months payment reserves</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="paymentReserves"><%= typeof solicitud.paymentReserves === 'number' ? solicitud.paymentReserves.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.paymentReserves %></span></td></tr>
                  <tr><td>Construction budget delta</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="budgetDelta"><%= typeof solicitud.budgetDelta === 'number' ? solicitud.budgetDelta.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.budgetDelta %></span></td></tr>
                  <tr><td>Total</td><td style="text-align: right;">$&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span id="totalLiquidity"><%= typeof solicitud.totalLiquidity === 'number' ? solicitud.totalLiquidity.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : solicitud.totalLiquidity %></span></td></tr>
                </table>
                <div style="height: 150px"></div>
                <table cellpadding="2" cellspacing="0" border="0" style="width: 100%; border-collapse: collapse">
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td style="width: 60%">GUARANTOR SIGNATURE:</td><td style="width: 40%"><span id="guarantorSignature"><%= solicitud.guarantorSignature %></span></td></tr>
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td>GUARANTOR NAME:</td><td><span id="guarantorName"><%= solicitud.guarantorName %></span></td></tr>
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td>DATE:</td><td><span id="guarantorDate"><%= solicitud.guarantorDate %></span></td></tr>
                  <tr style="background-color: #ffc55a; font-weight: bold; text-align: center;"><td></td><td></td></tr>
                </table>
              </td>
            </tr>
            <!-- Título -->
            <tr><td colspan="2" style="height: 8px"></td></tr>
            <tr>
              <td colspan="2" style="background-color: #000; text-align: center; font-weight: bold; font-size: 9px; padding: 8px; border: 1px solid #000;">
                <p style="color: #fff; margin: 0">"PLEASE EXECUTE THIS LETTER OF INTENT AND RETURN TO REINVESTAR CAPITAL GROUP SHOULD YOU WISH TO PROCEED Offer will expire within five (5) business days of the issued date."</p>
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html> 